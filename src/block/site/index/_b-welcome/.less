.b-welcome {
	
	#fe.bg(#ffffff);
	#fe.color(@c-text);
	
	@keyframes welcome-title-stick {
		from {
			left:-300%;
		}
		to {
			left:0;
		}
	}
	
	.welcome-title {
		position:relative;
		max-width:630px;
		margin-bottom:1em;
		#fe.size(6em);
		#fe.ff(pnb);
		
		&:before {
			content:'';
			position:absolute;
			left:-100%;
			bottom:-0.5em;
			#fe.w(60px);
			#fe.h(4px);
			#fe.bg(#009a81);
			animation-duration:@trans-speed * 1.5;
			animation-name: welcome-title-stick;
			animation-delay:@trans-speed * 1;
			animation-fill-mode:forwards;
		}
	}
	.welcome-about {
		max-width:670px;
		#fe.size(2.4em);
		#fe.lh(1.33333333em);
		
		a {
			#fe.color(@c-text);
			#fe.underline();
			
			&:hover {
				#fe.underline(none);
			}
		}
	}
}
