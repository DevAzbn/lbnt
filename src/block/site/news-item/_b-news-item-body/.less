.b-news-item-body {
	
	.news-item-cont {
		margin:0 auto;
		max-width:945px;
		#fe.w(100%);
		
		.title {
			position:relative;
			padding:1em 0;
			//#fe.ff(pnb);
			#fe.bold();
			#fe.size(3.6em);
			
			&:before {
				content:'';
				position:absolute;
				left:0;
				bottom:0;
				#fe.h(2px);
				#fe.w(54px);
				#fe.bg(#2f9981);
			}
		}
		.date {
			padding:2.2em 0 3em;
			#fe.color(#656565);
			#fe.size(1.8em);
		}
		.main_info {
			
		}
		.blog-gallery{
			margin:4em 0;
			#fe.h(600px);
		}
	}
	
}