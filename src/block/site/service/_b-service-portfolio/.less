.b-service-portfolio {
	display:block !important;
	
	.owl-container {
		max-width:100%;
		#fe.w(100%);
		
		.owl-carousel {
			max-width:100%;
			#fe.w(100%);
			
			.owl-controls {
				display:none;
				
				@media @media-size-xs {
					display:none;
				}
			}
			.item {
				display:block;
				position:relative;
				overflow:hidden;
				#fe.h(27em);
				#fe.bpos();
				#fe.brep();
				#fe.bsize(cover);
				
				&:hover {
					&:before {
						bottom:0%;
					}
					.title {
						bottom:10%;
					}
				}
				
				&:before {
					content:'';
					position:absolute;
					left:0;
					right:0;
					top:0;
					bottom:100%;
					transition:all @trans-speed ease;
					#fe.bg(rgba(255,255,255,0.33));
				}
				
				.title {
					left:10%;
					bottom:-100%;
					position:absolute;
					transition:all @trans-speed ease;
					#fe.color(#000000);
					#fe.size(2em);
					#fe.w(80%);
				}
			}
			
		}
	}
}