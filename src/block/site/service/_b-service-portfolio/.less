.b-service-portfolio {
	display:block !important;
	
	.owl-container {
		max-width:100%;
		#fe.w(100%);
		
		.owl-carousel {
			max-width:100%;
			#fe.w(100%);
			
			.owl-controls {
				display:none;
				
				@media @media-size-xs {
					display:none;
				}
			}
			.item {
				display:block;
				position:relative;
				overflow:hidden;
				#fe.h(27em);
				#fe.bpos();
				#fe.brep();
				#fe.bsize(cover);
				
				&:hover {
					.title {
						bottom:10%;
					}
				}
				
				.title {
					left:10%;
					bottom:-100%;
					position:absolute;
					transition:all @trans-speed ease;
					#fe.color(#ffffff);
					#fe.size(2em);
					#fe.w(80%);
				}
			}
			
		}
	}
}