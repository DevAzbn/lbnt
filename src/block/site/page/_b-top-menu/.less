.b-top-menu {
	position:fixed;
	left:0;
	top:0;
	padding:2em 0;
	#fe.w(100%);
	#fe.bg(transparent);
	#fe.color(@c-text);
	#fe.z(5);
	
	.site-menu, .contact-menu {
		#fe.lh(13px);
		#fe.lh(3em);
		
		ul {
			list-style-type:none;
			
			li {
				display:inline-block;
				margin:0 1.5em;
				
				&:first-child {
					margin-left:3em;
				}
				&:last-child {
					margin-right:3em;
				}
			}
		}
		
		a {
			#fe.uppercase();
			#fe.color(@c-text);
			#fe.size(1.3em);
		}
		
	}
	
	.site-menu {
		float:left;
		
		a {
			
			
			&.order-service-btn {
				display:inline-block;
				padding:0 20px 0;
				border-radius:15px;
				transition:all @trans-speed ease;
				#fe.bg(#2f9981);
				#fe.color(#ffffff);
				#fe.underline(none);
				
				&:hover {
					#fe.bg(transparent);
					#fe.color(#2f9981);
				}
			}
		}
	}
	
	.contact-menu {
		float:right;
		
		a {
			#fe.bold();
		}
	}
	
	a {
		#fe.underline(none);
		
		&:hover {
			#fe.underline();
		}
		
	}
}