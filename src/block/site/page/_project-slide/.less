.project-slide {
	transition:all @trans-speed ease;
	#fe.w(100%);
	#fe.bpos();
	#fe.brep();
	#fe.bsize(cover);
	
	.title {
		#fe.ff(pnb);
		#fe.size(3.5em);
	}
	.preview {
		#fe.ff(pnl);
		#fe.size(1.3em);
	}
	.link {
		position:relative;
		#fe.uppercase();
	}
}